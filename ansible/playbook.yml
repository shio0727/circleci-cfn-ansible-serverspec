---
- hosts: all
  become: yes
  tasks:
    - name: Install Git
      ansible.builtin.yum:
        name: git
        state: present
        lock_timeout: 180
  role:
    - { role: yum }
  vars: 
    app_dir: /home/ec2-user/raisetech-live8-sample-app